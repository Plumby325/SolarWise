# DAILY LOG - 2026-06-07

## 오늘 목표
- `FRONTEND_INTEGRATION_GUIDE` 및 `DASHBOARD_DATA_FLOW` 기준으로 프론트엔드 대시보드/예측 화면을 문서 요구사항에 맞게 정렬
- 타임라인 스키마를 문서 기준(`measuredAt/powerKw/detectedAt/lastUpdatedAt`)으로 통일
- 폴링을 하이브리드(기본 5초, playback 중 1초)로 재구성
- 타임라인 마커 클릭 상세/상태 변경 흐름 연결

## 방금 수행한 작업 요약

### 1) 타임라인 API 정규화 레이어 재작성
- `src/api/dashboard.ts`의 `DashboardTimelineResponse` 및 정규화 로직을 문서 기준 필드명으로 전환
  - `actualSeries`: `measuredAt`, `powerKw`
  - `predictionSeries`: `measuredAt`, `powerKw`
  - `gapSeries`: `measuredAt`, `absoluteGap`, `gapRate`
  - `anomalyMarkers`: `eventId`, `detectedAt`, `severity`, `summary` (+ 선택 `type/status`)
  - `lastUpdatedAt` 추가
- 기존 백엔드/레거시 응답 호환을 위해 fallback 파싱 유지
  - `ts/value`, `absGap`, `detectedAt|ts`, snake_case/camelCase 병행 처리

### 2) 타임라인 폴링 훅 하이브리드 구조 반영
- `src/shared/hooks/useDashboardTimeline.ts` 개선
  - playback 상태 조회 결과에 따라 polling interval 전환
    - 정지: 5초
    - 실행: 1초
  - `lastUpdatedAt` 동일 시 불필요한 `timeline` state 갱신 방지
  - `SIMULATION_CHANGE_EVENT` 수신 시 즉시 재조회
  - 401/권한 오류 감지 시 폴링 중단 + 로그인 페이지 이동 처리

### 3) 대시보드 화면 타임라인 중심 구조로 전환
- `src/features/dashboard/components/DashboardPage.tsx` 리팩터링
  - 기존 계측 실시간 차트(1h/12h/1d) 의존도를 낮추고 타임라인 기반 렌더 중심으로 전환
  - `DAY/WEEK/MONTH` 범위 제어 연결
  - `virtualNow`, `1s fixed` 표시 추가
  - `buildDashboardTimelineChartOption` 연결
  - 마커 클릭 이벤트 연결 후 `TimelineAnomalyPanel` 표시
  - 패널에서 상태 변경 후 타임라인 refresh 연결
- `src/features/dashboard/components/DashboardPage.module.css`
  - 마커 패널 조건부 표시를 위한 단일 컬럼 레이아웃 클래스(`timelineBodySingle`) 추가

### 4) 타임라인 차트/패널 타입 정합성 정리
- `src/shared/charts/dashboardTimelineChartOption.ts`
  - `ts/value` 기반 로직을 `measuredAt/powerKw/detectedAt` 기준으로 전환
  - 마커 툴팁 표시 안정화
- `src/shared/charts/forecastComboChartOption.ts`
  - 타임라인 옵션 빌더의 필드 참조를 문서 기준으로 수정
  - `virtualNow` 정확 일치가 아닌 nearest index 방식으로 markLine 계산 보강
- `src/features/dashboard/components/TimelineAnomalyPanel.tsx`
  - marker fallback 값의 필드명을 `detectedAt` 기준으로 정합화

### 5) 발전량 예측 페이지 타임라인 훅 정렬
- `src/features/power-forecast/components/PowerForecastPage.tsx`
  - 페이지 자체 1초 폴링 로직 제거
  - `useDashboardTimeline` 공통 훅 사용으로 대시보드와 동일한 시뮬레이션 갱신 타이밍 사용
  - `forecastHorizon` 값을 `futureHours`로 매핑하여 타임라인 조회 파라미터에 반영

### 6) 사이드바 시뮬레이션 메타 정보 보강
- `src/shared/layout/DashboardSidebar.tsx`
  - `tickSeconds`, `stepHours` 상태 수집/표시 추가
  - `beforeunload` 시 playback 실행 중이면 stop 호출 시도
- `src/shared/layout/DashboardSidebar.module.css`
  - 시뮬레이션 메타 라인 스타일(`simulationMeta`) 추가

### 7) API 에러 메시지 개선
- `src/api/client.ts`
  - 에러 메시지에 HTTP 상태코드 prefix 추가 (`[401] ...`)하여 권한 오류 분기 신뢰도 향상

## 수정된 파일
- `SolarWise/src/api/client.ts`
- `SolarWise/src/api/dashboard.ts`
- `SolarWise/src/features/dashboard/components/DashboardPage.module.css`
- `SolarWise/src/features/dashboard/components/DashboardPage.tsx`
- `SolarWise/src/features/dashboard/components/TimelineAnomalyPanel.tsx`
- `SolarWise/src/features/power-forecast/components/PowerForecastPage.tsx`
- `SolarWise/src/shared/charts/dashboardTimelineChartOption.ts`
- `SolarWise/src/shared/charts/forecastComboChartOption.ts`
- `SolarWise/src/shared/hooks/useDashboardTimeline.ts`
- `SolarWise/src/shared/layout/DashboardSidebar.module.css`
- `SolarWise/src/shared/layout/DashboardSidebar.tsx`

## 검증 상태
- 중단됐던 검증을 재개하여 최종 확인 완료
- `ReadLints`: 대상 변경 파일 기준 오류 없음
- `npm run build`: 성공 (`tsc -b && vite build` 통과)
- 참고: 번들 크기 경고(500kB 초과 chunk)는 기존과 동일한 경고 수준이며 빌드 실패 원인은 아님

## 다음 권장 작업
1. 실제 시뮬레이션 시나리오 E2E 확인
   - playback 시작/정지에 따라 네트워크 폴링 주기(5초↔1초) 전환 확인
   - `dashboard/timeline` 실응답 기준 마커 클릭 → 패널 표시 → 상태 변경 API 연동 확인
2. 401 응답 강제 상황에서 폴링 중단 및 로그인 이동 동작 확인
3. 문서(`FRONTEND_INTEGRATION_GUIDE`) 기준으로 남은 UI 디테일(배지/문구/상태 텍스트) 최종 점검

---

작성자: 프론트엔드 작업 로그 (자동 생성)  
작성일: 2026-06-07
